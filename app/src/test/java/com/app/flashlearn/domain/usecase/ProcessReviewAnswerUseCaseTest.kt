package com.app.flashlearn.domain.usecase

// این فایل placeholder تست است — طبق سند ۰۳ باید Fake Repository ها نوشته بشن
// (FakeLearningStateRepository, FakeReviewRepository) تا این تست‌ها واقعاً کامپایل/اجرا بشن.
// عمداً کامل ننوشتمش چون بدون آن‌ها فقط یک تست تزئینی می‌شد، نه واقعی.
//
// نمونه تست‌های مورد نیاز (طبق سند ۰۳):
// - daily correct moves to weekly with 7 day offset
// - monthly wrong second time sets very hard
// - learned stage stays learned regardless of answer
class ProcessReviewAnswerUseCaseTest {
    // TODO: پیاده‌سازی Fake Repository ها و نوشتن تست‌های واقعی
}
